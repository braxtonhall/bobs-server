<label for="soft-deadline">deadline</label>
<input id="soft-deadline" type="date" name="softDeadline" min="<%= minDeadline.toLocaleDateString('fr-ca') %>" />

<label for="soft-deadline">*real* deadline</label>
<input id="soft-deadline" type="date" name="hardDeadline" min="<%= minDeadline.toLocaleDateString('fr-ca') %>" />
