export enum ViewingState {
	IN_PROGRESS = "in-progress",
	PAUSED = "paused",
	FINISHED = "finished",
}

export enum Scope {
	FOLLOWING = "following",
	EVERYONE = "everyone",
	INDIVIDUAL = "individual",
}
