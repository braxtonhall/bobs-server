import { UserSettings } from "../types";

const Shuffle = (props: { settings: UserSettings | null }) => {
	return (
		<>
			<p>This is where the shuffling goes</p>
		</>
	);
};

export default Shuffle;
