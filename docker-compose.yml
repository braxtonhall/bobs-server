version: "3.5"

services:
  server:
    image: git@github.com:braxtonhall/bobs-server.git:docker # TODO before merging this should be main
    ports:
      - "80:80"
      - "443:443"
    restart: always
  dev-server:
    build:
      context: ./
    ports:
      - "8080:80"
      - "8443:443"
    restart: always
